# Copie este arquivo para .env (o .env nunca vai para o Git).

# Leitor de placa (Plate Recognizer)
PLATE_RECOGNIZER_TOKEN=

# Código do leitor de placa (opcional). Se preencher, o site pede o código uma vez em cada aparelho.
ACCESS_CODE=

# Chave da limpeza diária das fotos (a Vercel manda sozinha; local só para testar /api/limpeza).
CRON_SECRET=

# Banco (Supabase, projeto "Strike Details DATABASE")
SUPABASE_URL=https://afngfcclipuuptskoowh.supabase.co
SUPABASE_KEY=sb_publishable_erohIsYujk3va_V9v5RjRA_PnxYIzIc
# Chave secreta: o valor está na Vercel (variável SD_CHAVE_BANCO) e na tabela privado.sd_config do banco.
SD_CHAVE_BANCO=
